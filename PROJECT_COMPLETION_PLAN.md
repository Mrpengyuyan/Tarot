# Tarot Unity 项目完结计划书

> 版本：1.0
> 编制日期：2026-08-27
> 当前基线：Unity 6.3 LTS（6000.3.16f1）/ URP / Phase 64 / 版本 0.9.0
> 适用目录：`UnityTarot/UnityClient/TarotUnity`

**Goal:** 将当前塔罗牌仪式原型完成为一款可下载、可独立运行、具有完整 3D 卡牌演出和稳定 AI 解读闭环的单机塔罗牌游戏。

**Architecture:** Unity 负责桌面客户端、3D 场景、卡牌交互和演出；FastAPI 负责账户/访客会话、塔罗牌抽取、AI 解读、预算限制和安全边界；AI API Key 只保留在后端，不进入 Unity 客户端或公开仓库。

**Tech Stack:** Unity 6000.3.16f1、URP、C#、Input System、TextMeshPro、FastAPI、SQLAlchemy、现有 React/FastAPI Web 业务代码、GitHub Releases。

**Spec:** 本计划基于 `UNITY_FRONTEND_PLAN.md`、`UnityClient/TarotUnity/README.md`、`UnityClient/TarotUnity/Docs/PROJECT_CHRONICLE.md`、Phase 60-64 文档以及当前仓库实际代码和资源状态制定。

## 一、完成目标

### 1.1 产品定位

本项目的 1.0 目标不是复制《炉石传说》的具体美术、文案、角色、卡牌或 IP，而是以它的完成度作为体验参考，做出具有以下特征的 3D 塔罗牌游戏：

- 进入游戏后能看到具有空间层次的牌桌、烛光、背景和氛围效果。
- 牌可以被选择、发牌、悬停、翻转、展示和重新开始，动作有明确反馈。
- 抽牌过程有镜头、灯光、粒子、音效和节奏，而不是静态表单页面。
- 用户可以完整完成一次塔罗牌仪式，并看到易读、可滚动的最终解读。
- 后端不可用时仍然可以离线体验本地解读，不会卡死在加载界面。
- 后端可用时，真实 AI 解读能够通过安全的服务器接口返回。
- 普通玩家不需要安装 Python、Conda、Docker 或 Unity 编辑器，下载发行包后即可运行。

### 1.2 1.0 的明确边界

为了让项目真正完结，1.0 先完成“单人 3D 塔罗牌仪式”而不是无限扩张成完整多人卡牌对战平台。以下内容不作为 1.0 发布阻塞项：

- PvP、联机匹配、实时战斗和排行榜。
- 复杂的卡组构筑、法力值、随从、战斗回合和竞技场系统。
- 移动端适配、主机适配和跨平台账号同步。
- 大规模卡牌收集、成就、商城和付费系统。

这些内容可以作为 1.5/2.0 版本，但不应在 1.0 收尾阶段打断当前主线。

### 1.3 完结定义

当以下条件全部满足时，项目才进入 1.0 完结状态：

- 从 GitHub 干净拉取后，文档能够指导开发者和玩家正确区分 Unity 客户端、后端服务和发行包。
- Unity 客户端在 macOS Apple Silicon 和 Windows x64 至少各有一个可运行发行包。
- 1 张、3 张和 10 张牌阵都可以完整走通：进入、提问、抽牌、翻牌、生成解读、查看结果、重新开始。
- 本地模式和真实后端模式都经过实际运行验证；真实后端验证不能只依赖模拟服务器。
- 客户端不包含 API Key、数据库密码、管理员令牌或其他服务端秘密。
- 后端具备请求超时、输入长度限制、频率限制、单用户预算限制和服务异常降级。
- 当前 EditMode、PlayMode、后端集成和发行包冒烟测试均有最新记录。
- 在没有后端或网络断开时，用户能看到明确提示并继续使用本地模式。
- 项目不存在阻塞启动的 Console Error，主要交互不依赖开发者手动修改 Inspector。

## 二、当前状态基线

### 2.1 已经完成的部分

- Unity 6.3 LTS URP 项目已经创建，Build Settings 已包含 Boot、MainMenu、ReadingRoom、Result 四个场景。
- 已经形成 Midnight Parlor 视觉方向，包含牌桌、烛光、占卜球、帷幕、暗红和金色氛围。
- 已接入 78 张高清 Rider-Waite-Smith 卡牌图片、卡牌背面、音效和多组仪式 VFX。
- 已有 1/3/10 张牌阵的数据驱动结构，包含 Celtic Cross 十张牌布局。
- 已有发牌、洗牌、悬停、翻牌、结果展示、滚动文本和重新开始的基础流程。
- 已有本地占卜模拟器、后端客户端、后端阅读服务和运行时后端地址配置。
- 已有 300 个以上的 Unity 测试记录和纵向流程测试基础。
- 当前视觉版本已经达到“可展示原型”阶段，下一步重点应从继续堆视觉效果转向产品闭环和发行可靠性。

### 2.2 当前主要缺口

- Unity 正常启动流程没有玩家可操作的登录或访客会话入口，因此默认会走本地模式，真实 AI 闭环尚未完成用户级验证。
- 客户端配置默认指向 `http://localhost:8000/api/v1`，朋友下载后不能直接访问开发者电脑上的后端。
- GitHub 当前 `main` 主要是 Unity 客户端树，旧的 FastAPI/React 代码位于另一个本地仓库，代码仓库边界和发布方式需要统一。
- 现有后端集成测试主要使用本地模拟 HTTP 服务，不能替代真实 FastAPI 服务和真实部署环境验证。
- `TestResults` 中的通过记录早于 Phase 63-64，必须重新执行当前代码的测试并保存新报告。
- 当前工作区仍有 Unity 字体重新导入文件、工具目录生成图片和 Python 缓存等未整理文件。
- README 和部分阶段文档落后于 Phase 61-64，玩家安装、后端配置和发行流程还没有形成一份权威说明。
- 现有运行时部分状态文案仍然是英文，中文界面的一致性和异常提示需要统一。
- 当前工程没有可交付的 macOS/Windows 发行包留在仓库中，发布流水线和干净机器验证尚未完成。

## 三、总体路线图

| 阶段 | 优先级 | 目标 | 主要产出 | 完成判断 |
| --- | --- | --- | --- | --- |
| Phase 0 | P0 | 冻结范围并统一仓库与发布架构 | 仓库决策、版本基线、清洁工作区 | 团队知道唯一代码入口和唯一启动方式 |
| Phase 1 | P0 | 打通真实后端和会话流程 | 访客/登录会话、真实 API 冒烟流程 | 玩家可以从 Unity 完成真实 AI 解读 |
| Phase 2 | P0 | 固化卡牌和牌阵核心玩法 | 78 张卡牌数据、稳定状态机、三种牌阵 | 三种牌阵在异常输入下仍可完成 |
| Phase 3 | P1 | 完成 3D 卡牌游戏级演出 | 厚度、翻牌、镜头、灯光、VFX、音频 | 抽牌过程具有完整节奏和反馈 |
| Phase 4 | P0 | 完善结果页和 AI 失败降级 | 结果阅读体验、重试、超时、离线降级 | 解读可读、可恢复、不会卡死 |
| Phase 5 | P1 | 完成成本、安全和玩家体验 | 配额、限流、配置、中文化、设置 | 可控成本地开放给朋友试玩 |
| Phase 6 | P0 | 完成质量验证和跨平台发行 | 测试报告、发行包、安装说明 | 干净环境可下载并直接游玩 |
| Phase 7 | P0 | 1.0 验收和发布 | Release、版本说明、回滚方案 | 达到完结定义并可公开发布 |

依赖关系为：`Phase 0 -> Phase 1/2 -> Phase 3/4/5 -> Phase 6 -> Phase 7`。Phase 3 的视觉细化不能替代 Phase 1 的真实服务闭环；否则会得到“看起来完成，但朋友无法游玩”的版本。

## 四、分阶段执行计划

### Phase 0：范围冻结与仓库归一化

**目标：** 先消除两个本地仓库、一个 GitHub 地址和多个运行方式造成的发布歧义。

#### 任务

1. 以当前 Unity Phase 64 作为视觉基线，创建一个明确的 `0.9.0` 基线记录，后续改动都从该基线开始。
2. 决定 GitHub 的最终组织方式。推荐保留一个产品仓库，至少包含 `UnityClient/`、后端源代码目录、部署文档和发行说明；不要让 Unity 客户端与后端分别指向同一个仓库地址却互相看不到。
3. 将旧 Tarot 仓库中的 FastAPI、数据库迁移、依赖文件、后端测试和必要的配置说明纳入最终交付方案，保留现有测试历史，不直接覆盖已有代码。
4. 统一根目录 README，明确三种模式：本地 Unity 开发、本地后端联调、玩家发行包。
5. 清理或忽略 Unity 重新生成的临时文件、工具目录 Python 缓存和不应提交的本地产物；字体资源先确认是否是 Unity 正常生成结果，再决定是否纳入版本控制。
6. 建立版本标签策略：开发版本使用 `0.9.x`，第一个可公开试玩版本使用 `1.0.0`，以后按功能和修复分别递增。

#### 涉及文件

- `UNITY_FRONTEND_PLAN.md`
- `UnityClient/TarotUnity/README.md`
- `.gitignore`
- 旧 Tarot 仓库中的 `README.md`、`requirements.txt`、`app/`、`tests/` 和前端启动文件

#### 交付物与验收

- 一份根目录架构说明，能够回答“后端在哪里、Unity 在哪里、朋友下载什么、API Key 放在哪里”。
- 一条主分支和一个明确的发行入口，后续不再从过期的本地仓库提交。
- 清洁工作区检查通过，未追踪文件只包含明确允许保留的资源。

### Phase 1：真实后端与会话闭环

**目标：** 让 Unity 从启动到 AI 结果真正连接到后端，而不是只在测试模拟器中成功。

#### 任务

1. 设计并实现 Unity 端的会话入口。推荐 1.0 使用“访客会话优先，正式登录可选”的方式：玩家第一次启动即可获得受限访客会话，不要求朋友先注册；需要更高额度时再提供登录能力。
2. 如果后端暂时不能提供访客会话，则先实现清晰的登录/注册页面，并在未登录时明确提供本地模式入口，不能静默失败。
3. 把会话创建、令牌保存、令牌失效、退出登录、重新尝试和服务端错误映射纳入 `ApiClient` 与 UI，而不是让 `ReadingRoomController` 直接承担全部状态管理。
4. 为后端增加健康检查接口或等价的启动探测，使客户端能区分“网络不可用”“服务未启动”“会话失效”和“AI 生成失败”。
5. 校验 Unity 的牌阵 ID、问题字段、卡牌方向、牌阵位置和后端响应字段完全一致。1 张、3 张、10 张必须分别使用真实后端跑通。
6. 对 `CreateRecord -> DrawCards -> GetRecordCards -> CreateInterpretation -> GetRecord` 链路做真实 FastAPI 集成测试，并至少进行一次真实部署环境测试。
7. 保留本地模拟模式，但把它明确标记为离线体验，不把本地占卜文案误认为真实 AI 结果。
8. 将运行时服务地址继续放在配置文件或环境覆盖中；客户端配置只允许出现服务地址和超时时间，不允许出现 DeepSeek Key。

#### 推荐新增或调整的文件

- `UnityClient/TarotUnity/Assets/Scripts/Network/ApiClient.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Network/BackendReadingService.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Network/ApiSessionState.cs`
- `UnityClient/TarotUnity/Assets/Scripts/UI/SessionController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/UI/ServiceStatusPresenter.cs`
- `UnityClient/TarotUnity/Assets/StreamingAssets/tarot_desktop_config.json`
- 后端认证、健康检查和阅读接口文件及对应测试

#### 交付物与验收

- 新用户启动发行包后可以进入离线模式。
- 新用户在后端在线时可以获得会话并完成真实 AI 解读。
- 后端关闭、超时、返回 401、返回 429、返回 5xx 时，客户端都有可理解的提示和恢复动作。
- 对客户端和提交历史进行秘密扫描，确认没有 API Key 和令牌。

### Phase 2：卡牌数据与核心玩法固化

**目标：** 让卡牌不是“几个演示对象”，而是稳定、可扩展、可验证的核心游戏数据。

#### 任务

1. 建立完整 78 张牌的唯一数据目录，统一牌名、编号、图片、正位含义、逆位含义和牌阵位置。
2. 将卡牌抽取从当前本地占卜模拟器的占位循环改为真正的无重复洗牌；本地模式也应覆盖 78 张牌，而不是只循环少量占位牌。
3. 固化卡牌状态机：未出现、待发牌、已发牌、悬停、可翻牌、翻牌中、正面、逆位、结果展示。禁止通过多个布尔值产生互相矛盾的状态。
4. 统一鼠标、触控板和键盘输入，确保用户知道下一张牌是否可以翻开，以及当前还剩多少张未翻开。
5. 验证 1/3/10 张牌阵的布局、发牌顺序、卡牌方向和结果顺序一致；特别检查 Celtic Cross 的十个位置在 ReadingRoom 与 Result 中不发生错位。
6. 处理重复点击、快速点击、窗口失焦、重新开始、切换场景和网络返回过晚等边界情况。
7. 为每张牌和每种牌阵补充数据级测试，不把全部正确性寄托在手工观察场景。

#### 推荐涉及的现有文件

- `UnityClient/TarotUnity/Assets/Scripts/Data/ApiContracts.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Data/ReadingSessionMapper.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/CardView.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/CardFlipController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/DeckController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/ReadingFlowController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/LocalReadingSimulator.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/SpreadCatalog.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/SpreadDefinition.cs`

#### 交付物与验收

- 78 张牌的资源、数据和显示名称一一对应。
- 三种牌阵连续重复体验 20 次，不出现重复卡牌、漏牌、错位、无法翻牌或结果错位。
- 输入异常不会跳过牌、重复生成结果或把用户带入不可恢复状态。

### Phase 3：3D 卡牌游戏级演出

**目标：** 在现有 Midnight Parlor 基础上，进一步达到“有空间、有重量、有节奏”的 3D 卡牌体验。

#### 任务

1. 将卡牌表现从平面图片叠加逐步升级为有厚度的 3D 卡牌：正面、背面、边缘、阴影和反光应在镜头移动时保持可信。
2. 为悬停、选中、发牌、翻牌、正位、逆位和结果锁定分别定义动作曲线、音效和视觉反馈，避免所有动作只使用同一种缩放动画。
3. 制作完整的“洗牌到发牌到翻牌到结果”的镜头节奏：准备、等待、动作、停顿、确认、下一步，每一段都能被玩家感知。
4. 继续使用当前烛光、占卜球、帷幕、桌面和后处理方向，但将粒子数量、透明材质和实时灯光控制在桌面设备可接受的性能预算内。
5. 为 10 张牌阵设计专用镜头和布局，确保横向空间不足时仍然能看清牌位、编号和交互提示。
6. 增加音量、静音和基础质量选项，避免玩家无法关闭持续音效或在低性能设备上被过多特效拖慢。
7. 对卡牌、牌桌和特效资源核对授权来源，发行包只使用项目拥有或允许再分发的资源。

#### 推荐涉及的现有文件

- `UnityClient/TarotUnity/Assets/Scripts/Presentation/`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/CardView.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/CardFlipController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/Gameplay/DeckController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/UI/ReadingRoomController.cs`
- `UnityClient/TarotUnity/Assets/Scripts/UI/ResultPanelPresenter.cs`
- `UnityClient/TarotUnity/Assets/Art/MidnightParlor/`
- `UnityClient/TarotUnity/Assets/Audio/`

#### 交付物与验收

- 一段 1 分钟的完整抽牌录屏，能够展示启动、洗牌、发牌、翻牌和结果，不需要开发者手动修正场景。
- 鼠标悬停、点击和翻牌都有一致反馈；快速操作不会破坏镜头或卡牌层级。
- 在目标设备上保持稳定帧率，长时间运行没有明显内存增长或粒子失控。

### Phase 4：结果页与 AI 解读体验

**目标：** 把当前“能显示文字”升级为玩家真正愿意阅读、能够恢复和重新体验的结果页。

#### 任务

1. 统一最终答案的结构：问题概览、牌阵说明、每张牌的牌位、正逆位、关键词、单牌解读、整体结论和行动建议。
2. 将长文本显示拆为清晰段落和区块，保证中文字体、行距、滚动区域和窗口缩放下仍然可读。
3. 在 AI 生成期间显示当前阶段、预计等待状态和取消/返回能力；禁止只有一个永久旋转图标。
4. 为超时、空回答、格式错误、内容过滤、额度耗尽和服务暂时不可用设计不同提示。
5. 提供“重新解读”和“回到牌阵”动作，确保失败后不用强制退出整个游戏。
6. 保留本地降级结果，但在结果页清楚标识“离线模拟解读”，避免玩家误以为调用了真实 AI。
7. 对 AI 返回内容做长度上限、格式校验和安全显示，不把未经处理的 Markdown/HTML 当作 Unity UI 直接执行。
8. 将当前运行时英文状态文案迁移到统一中文文案表，避免中文界面中混入 `Creating backend reading...` 一类开发状态文本。

#### 交付物与验收

- 10 张牌阵的长解读在 1920x1080 和较小窗口中都能滚动阅读。
- AI 失败后可以重试或进入本地模式；不会出现黑屏、空白结果或无限等待。
- 同一份解读在不同窗口尺寸下没有文字裁切、按钮遮挡和滚动区域失效。

### Phase 5：安全、成本与玩家体验

**目标：** 使用开发者自己的 API Key 也能让朋友试玩，同时把成本和滥用风险控制在可接受范围内。

#### 服务器安全原则

- DeepSeek 或其他模型的 API Key 只存在后端环境变量、密钥服务或部署平台的安全配置中。
- Unity `StreamingAssets`、安装包、日志、异常堆栈和 Git 历史都不能出现 API Key。
- 客户端只知道 HTTPS 后端地址、超时时间和公开版本配置。
- 生产服务必须使用 HTTPS；不把开发者电脑的 `localhost` 作为朋友发行版的默认地址。

#### 成本控制任务

1. 为访客会话设置每日阅读次数上限，并为正式账户设置独立额度；限制值由后端配置，不写死在客户端。
2. 限制问题长度、牌阵规模、单次输出长度和请求超时时间，防止单次请求消耗失控。
3. 增加 IP、设备会话和账户级频率限制；多实例部署时把计数放入共享存储，而不是只放在单个进程内存。
4. 对相同问题、相同牌阵和相同牌面组合评估缓存策略，缓存内容不应泄漏用户隐私。
5. 为模型选择建立明确策略：默认使用成本可控的模型，只有在确有必要时才允许更高成本模型。
6. 记录请求耗时、状态码、输入长度、输出长度、失败原因和总额度消耗，但日志中不记录 API Key 和完整隐私问题。
7. 设置每日/每月预算告警和紧急关闭开关；达到预算时返回本地模式或友好提示，不继续无限调用。
8. 在游戏中加入简单的 AI 使用说明和免责声明，说明结果是娱乐性质的生成内容。

#### 玩家体验任务

- 首次启动明确选择“在线 AI 解读”或“离线体验”，不要用隐蔽的开关控制主要流程。
- 提供后端连接状态、重试和切换本地模式入口。
- 提供音量、全屏/窗口、画质和退出设置。
- 统一简体中文文案，并检查字体在发行包中可用。
- 对 16:9、窗口缩放、中文长文本和低分辨率进行实际验收。

#### 交付物与验收

- 后端预算与限流配置说明。
- 一次完整的 API Key 泄漏扫描结果。
- 模拟 50 个访客请求时不会突破设定配额；超过配额后能稳定降级。
- API Key 被移除或后端关闭时，发行包仍然能进入本地模式。

### Phase 6：测试、性能和发行

**目标：** 把“开发机上能运行”变成“其他人下载后能运行”。

#### 测试矩阵

1. Unity EditMode：数据模型、牌阵布局、映射、状态机、配置解析、错误处理。
2. Unity PlayMode：MainMenu -> ReadingRoom -> 抽牌 -> 翻牌 -> Result -> 重新开始的纵向流程。
3. 后端单元测试：认证、牌阵、预算、限流、错误响应和 AI 服务异常。
4. 真实集成测试：Unity 客户端连接实际 FastAPI 部署，完成 1/3/10 张真实阅读。
5. 离线测试：后端关闭、断网、慢响应、401、429、5xx、空回答和格式错误。
6. 发行包冒烟测试：从压缩包解压到首次启动、设置、完成一次本地阅读、退出程序。
7. 安全测试：仓库历史、构建目录、日志和配置中没有 API Key、密码和令牌。
8. 性能测试：启动时间、场景切换时间、峰值内存、卡牌动画帧率、长文本滚动和 10 张牌阵特效峰值。

#### 发行任务

- 将 `ProjectSettings/ProjectSettings.asset` 中的公司名、产品名、版本号、应用标识和默认窗口设置改为正式值。
- 生成 macOS Apple Silicon 发行包和 Windows x64 发行包；如需 Intel Mac，单独验证 Universal 或 Intel 构建，不默认宣称支持。
- 为每个发行包生成校验值，避免玩家下载损坏文件。
- 编写中文安装和启动说明，说明在线模式需要什么、离线模式能做什么、遇到网络问题如何处理。
- 将发行包放入 GitHub Release，不把大型构建产物直接塞进源码提交历史。
- 创建 `v1.0.0` Release，附带版本说明、已知限制、隐私说明和后端状态页地址。

#### 交付物与验收

- 新 Mac 用户和新 Windows 用户各完成一次干净安装测试。
- 不安装 Python、Conda、Docker 或 Unity 编辑器也能启动客户端。
- 从 GitHub Release 下载到运行完成不需要开发者手动改配置文件。
- 测试报告记录当前提交号、Unity 版本、操作系统、测试数量和失败项。

### Phase 7：1.0 发布验收

#### 发布前检查

- [ ] 代码仓库边界唯一，README 与实际目录一致。
- [ ] Unity 项目可用 Unity 6000.3.16f1 打开并正常导入。
- [ ] Boot、MainMenu、ReadingRoom、Result 四场景加入 Build Settings。
- [ ] 1/3/10 张牌阵均可完成本地模式流程。
- [ ] 1/3/10 张牌阵均完成真实后端流程验证。
- [ ] 真实服务不可用时可切换本地模式。
- [ ] API Key 只存在后端安全配置中。
- [ ] 后端已配置超时、限流、额度、预算告警和紧急关闭。
- [ ] 运行时没有阻塞性 Console Error。
- [ ] 中文文案、字体、结果滚动和按钮在目标窗口尺寸可用。
- [ ] macOS 和 Windows 发行包已在干净环境启动。
- [ ] EditMode、PlayMode、后端测试和发行冒烟测试均有最新报告。
- [ ] 卡牌、字体、音乐、背景和特效资源具备再分发依据。
- [ ] GitHub Release、版本说明、安装说明和已知限制已经发布。

#### 发布后第一周

1. 收集启动失败、后端错误、平均响应时间和每日调用量。
2. 观察预算消耗和访客额度是否合理，必要时优先调整服务端配置而不是立即改客户端。
3. 记录玩家最常见的卡牌交互问题和阅读体验问题。
4. 只修复影响启动、流程完成、成本和安全的高优先级问题；新增功能进入 1.1 计划。

## 五、下一步立即执行顺序

当前最合理的下一步不是继续增加粒子或装饰，而是按以下顺序收尾：

1. **先完成 Phase 0：** 统一 GitHub 仓库结构，确认后端源代码与 Unity 客户端的最终归属，清理当前工作区并补齐权威 README。
2. **再完成 Phase 1：** 增加访客/登录会话入口，配置真实后端地址，使用真实 FastAPI 服务走通 1/3/10 张牌阵。
3. **然后完成 Phase 2：** 固化 78 张牌数据、无重复洗牌、卡牌状态机和牌阵映射。
4. **接着完成 Phase 4 和成本控制：** 先让结果可靠、可恢复、可读，再开放给朋友使用。
5. **最后集中做 Phase 3 的视觉收尾与 Phase 6 的发行：** 视觉打磨必须建立在流程和服务稳定的基础上。

第一轮开发验收点应是：**新用户打开 Unity 发行包，获得访客会话，选择三张牌阵，输入问题，完成真实 AI 解读；即使服务失败，也能明确切换到本地模式并完成流程。** 这个验收点通过后，项目才从“演示原型”进入“可发行游戏”阶段。

## 六、后续版本方向

### 1.1：内容和沉浸感扩展

- 更多牌阵、更多独立牌桌主题和可替换音乐。
- 解读历史、收藏和分享结果。
- 更丰富的卡牌材质、镜头语言和可选演出速度。
- 更完善的设置、无障碍和语言支持。

### 2.0：真正的卡牌游戏系统

只有在 1.0 稳定后，才评估以下方向：

- 牌组、卡牌收集、卡牌升级和构筑系统。
- 回合制规则、资源系统和对战场景。
- AI 对手、单人关卡或联网 PvP。
- 账号云同步、排行榜和跨平台发行。

这些内容会改变项目的后端、数据模型、服务器成本和测试规模，不应被误认为是当前 1.0 收尾工作的自然延伸。

## 七、主要风险与处理原则

| 风险 | 影响 | 处理原则 |
| --- | --- | --- |
| API Key 被放入客户端 | 高额费用和密钥泄漏 | Key 只在后端，客户端只访问 HTTPS 接口 |
| 朋友无法访问 `localhost` | 发行包无法完成在线解读 | 提供部署后的 HTTPS 后端地址，并保留离线模式 |
| 两个本地仓库指向同一远程 | 代码丢失或发布错版本 | Phase 0 先统一仓库边界和提交入口 |
| AI 响应慢或失败 | 玩家卡在结果页 | 超时、重试、本地降级和明确错误状态 |
| 10 张牌阵空间不足 | 牌面和牌位不可读 | 专用镜头、布局缩放和结果页横向滚动 |
| 特效过多造成卡顿 | 沉浸感反而下降 | 设定粒子、灯光、透明材质和内存预算 |
| 资源授权不清 | 无法公开发行 | 建立资源来源和再分发记录 |
| 文档落后于代码 | 新用户无法启动 | 每个阶段完成时同步 README 和版本说明 |

## 八、项目完结后的判断

如果只看当前画面，项目已经接近“可展示的 3D 塔罗牌原型”；如果按“朋友下载后即可玩”的标准，真正剩下的核心工作是：**统一仓库、完成真实会话与后端闭环、加入服务端成本控制、重新验证当前版本、生成跨平台发行包。**

因此，项目的完结路线应当是“先产品闭环，再视觉收尾，最后发行验证”，而不是继续无限增加美术阶段。完成本计划的 1.0 部分后，我们就拥有一个真正可交付的单人 3D 塔罗牌游戏；之后再以 1.1/2.0 的方式逐步靠近更完整的 3D 卡牌游戏体系。
